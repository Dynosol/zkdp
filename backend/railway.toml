[build]
builder = "nixpacks"
buildCommand = "cargo build --release && mkdir -p /out && cp target/release/dp_client /out/"

[deploy]
startCommand = "RUST_LOG=info PORT=8080 /out/dp_client"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variables]
PORT = "8080" 